> `[작업]` **이 폴더에서 만든 문서 — 원문에 없는 규칙·판단 포함 · 고쳐도 됨** (원문의 변환 프롬프트 인용)
# 생성기 — 토큰/글자수 최적화

> 출처: `00_가이드/챗봇/02_프롬프트짜기.md` (토큰 최적화)

## 판단 기준
| 플랫폼 관리 단위 | 권장 언어 |
|---|---|
| **토큰** 제한 | 영어 (한글은 같은 글자수라도 토큰을 훨씬 많이 먹음) |
| **글자수** 제한 | 한국어 (또는 압축률 좋은 중국어) |

⚠️ 한 프롬프트 칸 안에서 한/영/중을 **섞지 말 것.** 문맥 이탈·엉뚱한 언어 답변의 원인.

## 변환 프롬프트

Markdown으로:
```
해당 정보를 영어로 번역해서 Markdown 방식으로 구성해줘.
```

YAML로:
```
해당 정보를 영어로 번역해서 YAML 방식으로 구성해줘.
```

## 그래도 넘칠 때 (줄이는 순서)
1. 조연·용어·이벤트 상세 → **로어북**으로 이동 (메인에는 이름·소속만)
2. `# 세계관 핵심`, `# 톤 & 분위기` 등 간접적인 섹션 삭제
3. `# NSFW` (전체 이용가면 무조건 삭제)
4. `# 일과` — 실제로 대화에 안 쓰이면 축약
5. 줄글로 남은 문장 → `속성: 값` 으로 재압축
