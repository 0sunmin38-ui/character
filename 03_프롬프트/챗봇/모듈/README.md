> `[작업]` **이 폴더에서 만든 문서: 원문에 없는 규칙·판단 포함 · 고쳐도 됨**
# 챗봇: 재사용 모듈

여러 캐릭터에 공통으로 붙여 쓰는 블록. 캐릭터 전용 내용은 넣지 않는다.

| 폴더 | 내용 |
|---|---|
| `출력규칙/` | `# System Rule`, `# 출력 규칙`. {{user}} 대리 서술 금지, 장면 스킵 규칙, 답변 길이 등 |
| `문체지침/` | `# 문체`. `00_가이드/챗봇/` 50선에서 뽑아 압축한 지시문 세트 |
| `커맨드_상태창/` | `!상태`, `!SNS` 등 커맨드 정의와 상태창 출력 포맷. 대부분 로어북에 넣는다 |

파일명은 **용도가 드러나게**: `건조단문_3인칭.md`, `상태창_호감도바.md`

## 지키는 것
- 캐릭터 이름을 하드코딩하지 말 것 → `{{char}}` / `{{user}}`
- 모듈 하나 = 붙여넣기 단위 하나. 여러 규칙을 한 파일에 섞지 말 것
- `OOC` 표기 금지
