# character — AI 캐릭터 챗봇 & 이미지 프롬프트 작업 폴더

AI 캐릭터 채팅 플랫폼에 올릴 캐릭터의 **① 챗봇 프롬프트**와 **② 이미지 프롬프트**를 만든다.
사용자는 한국어로 작업한다. 답변도 한국어로.

## 폴더 = 작업 단계 × 도메인

| 폴더 | 성격 |
|---|---|
| `00_가이드/` | **읽는 것** — 방법론 원문·참고 문서(서술형 md) |
| `01_자료/` | **찾는 것** — 태그·링크 CSV. `.md` 를 두지 않는다 |
| `02_템플릿/` | 빈 양식. 복사해서 채운다 |
| `03_프롬프트/` | **최종 산출물** |
| `04_생성기/` | AI에게 시킬 메타 프롬프트 |
| `05_검토/` | 초안 판정 체크리스트 |
| `99_작업중/` | 초안 |
| `dcgall/` | 갤러리 수집기 (파이프라인 밖 도구) |

각 폴더는 `챗봇/` `이미지/` 로 갈린다. 새 파일은 이 격자 안 제자리에 놓는다.

## 문서 등급 — 첫 줄 배너로 판단

```
> `[원문]` 외부 글 전재 · 편집 금지
> `[정리]` 원문에서 뽑아 재구성 — 값·문구는 원문 것
> `[작업]` 원문에 없는 규칙·판단 — 고쳐도 됨
```

**`[원문]` 파일의 본문은 절대 수정하지 않는다.** 우리가 붙인 상단 안내 블록은 고쳐도 된다.
새 md 를 만들면 첫 줄에 배너를 붙이고 `문서구분표.md` 에 등록한다.

## 지켜야 할 규칙

- **같은 내용을 두 곳에 적지 않는다.** 정본을 정하고 다른 쪽엔 포인터만.
  주제별 정본표 → `00_가이드/README.md`
- **템플릿에는 구조만, 태그 값은 CSV에.** 태그 목록이 `02_템플릿/` 에 쌓이면 `01_자료/` 로 옮긴다.
- **태그는 CSV에 행을 추가**한다. 새 파일·폴더를 만들지 않는다.
- 부정/퀄리티 프롬프트는 `03_프롬프트/이미지/스타일프리셋/` 한 곳에서만 관리하고 `N번호`·`Q번호`로 부른다.
- 한 프롬프트 안에서 형식(Markdown/YAML/XML)과 언어(한/영/중)를 섞지 않는다.
- `OOC` 표기를 쓰지 않는다. `# System Rule` / `# 출력 규칙` 섹션으로 대체한다.
- 사용자 지칭은 `{{user}}`, 캐릭터는 `{{char}}`.

## 작업 흐름

**챗봇** — 컨셉 → `02_템플릿/챗봇/01,02` 채우기(자료) → `04_생성기/01` 로 하네스 8블록 재배치
→ `02_템플릿/챗봇/04,05,06` → `04_생성기/02` 토큰 최적화 → `05_검토/01` → `03_프롬프트/챗봇/캐릭터/<이름>/`

**이미지** — 작가태그 + 캐릭터 외형고정 + 26슬롯 장면 + 퀄리티/부정 프리셋
→ `05_검토/02` → `03_프롬프트/이미지/`

같은 캐릭터의 챗봇 폴더명과 이미지 파일명을 **같게** 맞춘다.

## 자주 쓰는 조회

```bash
D=01_자료/이미지
grep -i "안경"           $D/1-태그사전.csv      # 4,205행 · 한글명·태그 검색
awk -F, '$1=="태그의상"'  $D/1-태그사전.csv      # 시트로 좁히기
awk -F, '$6!="TRUE"'     $D/1-태그사전.csv      # 전체 이용가만
grep -i "watercolor"     $D/1-스타일태그.csv     # 88행 · 그림체 태그 뜻
```

- 태그의 언더바(`_`)는 실제 사용 시 띄어쓰기로 바꾼다.
- `1-태그사전.csv` 의 NSFW 공란 436행은 원본 시트에 값이 없는 것이다. `FALSE` 와 함께 다룬다.
- **AI가 지어낸 태그를 걸러내려면 반드시 `1-태그사전.csv` 와 대조한다** (`05_검토/02` A1).

## 하네스 8구조 (챗봇 프롬프트의 뼈대)

①Block ②Anchor ③Trigger ④Guard ⑤Fallback ⑥Scope ⑦Chain ⑧Layer
— 순서가 곧 우선순위. Anchor 를 항상 최상단에. 상세는 `02_템플릿/챗봇/03_하네스_8블록_템플릿.md`.

| 증상 | 원인 블록 |
|---|---|
| 대화가 길어지면 캐릭터가 흐릿해짐 | Anchor |
| 점점 착해지고 물러짐 | Fallback · Guard |
| 같은 상황에서 반응이 매번 다름 | Trigger (형용사로만 씀) |
| 설정에 없는 걸 지어냄 | Scope |
| 메타 발언이 대사에 섞임 | Layer |
