> `[작업]` **이 폴더에서 만든 문서: 원문에 없는 규칙·판단 포함 · 고쳐도 됨**
# 99_작업중

초안·실험용. 여기 있는 건 **아직 쓸 수 있는 상태가 아니다**라는 뜻.

> **캐릭터(챗봇) 프롬프트는 여기 없다.** 별도 저장소 `luvheilcharacter` 로 떨어져 나갔다.

```
99_작업중/
└── 이미지/     ← 뷰어의 이미지 → 조립 에서 저장한 것
```

## 뷰어에서 다루기

읽기 탭에는 나오지 않는다. 초안은 **`작업중` 탭**에서 본다.
거기서 바로 **수정·삭제**할 수 있다 (`node viewer/serve.mjs` 로 띄웠을 때).

## 03_프롬프트로 올리는 조건

한 곳에서만 정한다 → **`05_검토/README.md` 의 「쓰는 법」**

요약하면: 초안을 여기 두고 → 검토표(`05_검토/02`)를 순서대로 통과시키고 →
`03_프롬프트/README.md` 의 파일 머리말을 채운 뒤 옮긴다.

파일명 앞에 날짜를 붙이면 정리가 쉽다: `260828_아이디어_교사캐릭터.md`
