# ── 절대 커밋하면 안 되는 것 ────────────────────────────────
# 수집한 원본. 타인의 글과 이미지가 들어 있습니다.
data/

# 리포트 HTML. 수집한 글이 통째로 박혀 있습니다.
out/

# docs/ 는 GitHub Pages 용으로 일부러 커밋합니다 (node pages.mjs 로 만듦).
# 닉네임·uid·IP 를 뺀 읽기 전용 사본이고 noindex 가 걸려 있습니다.

# 공개용 내보내기 결과. 직접 확인하고 올릴 것.
public/

# 사전에서 자동 생성. 갤러리 글 제목이 예시로 들어갑니다.
GLOSSARY.md

# 이 기기의 데이터 폴더 위치
.datadir

# ── 잡동사니 ───────────────────────────────────────────────
.DS_Store
node_modules/
*.log
