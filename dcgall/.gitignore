# ── 절대 커밋하면 안 되는 것 ────────────────────────────────
# 수집한 원본. 타인의 글·이미지와 닉네임·uid·댓글 IP 조각이 들어 있습니다.
# 한 번 커밋하면 히스토리에서 지우기 까다로우니 처음부터 제외합니다.
data/

# ── 생성물 ─────────────────────────────────────────────────
out/          # 리포트 HTML (데이터가 통째로 박혀 있음)
public/       # 공개용 내보내기 결과 (직접 확인하고 올릴 것)
GLOSSARY.md   # 사전에서 자동 생성 (원하면 이 줄을 지우고 커밋)

# 이 기기의 데이터 폴더 위치 (기기마다 다름)
.datadir

# ── 잡동사니 ───────────────────────────────────────────────
.DS_Store
node_modules/
*.log
